[project]
name = "chatpdf"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=2.3,<4",
    "openai>=1.0,<2",
    "python-dotenv>=1.0,<2",
]
